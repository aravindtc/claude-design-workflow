# Layout rules

- nested divs → nested auto layout frames
- flex-row → horizontal auto layout
- flex-col → vertical auto layout
- justify-between → space between
- items-center → center alignment

Always:
- use auto layout instead of manual positioning
- preserve nesting hierarchy
- use spacing tokens instead of arbitrary values
